---
name: template-skill
description: Replace with a description of the skill and when the agent should use it.
---

# Insert instructions below

<!--
Conventions used by skills in this repo:

- `<SKILL DIR>` — the absolute path of the directory containing this `SKILL.md`.
  Reference asset paths as `<SKILL DIR>/<subdir>/<file>` so they resolve
  regardless of where the skill is installed (e.g., `~/.claude/skills/<name>/`
  or this repo's `skills/<name>/`).

- `<USER REQUEST>` — the developer's invocation arguments.

To register this skill in the bundle, add an entry under `skills/<name>/` and
list it in `.claude-plugin/marketplace.json`.
-->
