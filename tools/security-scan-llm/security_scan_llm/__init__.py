"""security_scan_llm — host-side LLM SAST lanes (codex + claude + gemma) + Projects v2 filer.

Complement to the deterministic GitHub Action (leverj/security-scanner). Both
substrates file into the same Projects v2 board; fingerprint scheme is shared so
findings dedup across substrates.
"""

__version__ = "0.2.0"
