## User Story

As a [role], I want [capability] so that [benefit].

## Acceptance Criteria

- WHEN [condition] THEN the system SHALL [expected behavior]
- WHEN [condition] THEN the system SHALL [expected behavior]

## Implementation Phases

- [ ] Phase 1: [description]
- [ ] Phase 2: [description]
- [ ] Phase 3: [description]

## Risk Assessment

- **Technical risks**: [what could go wrong technically]
- **Dependencies**: [what this depends on — other issues, services, decisions]
- **Unknowns**: [what we don't know yet]

## Notes

[Additional context, links, screenshots, related decisions]

## Assumptions

<!-- Assumption Ledger. Auto-populated by `/sprint pick` during implementation — one line per decision made without asking, per the Autonomy & Escalation Policy. Format: `- [TIER] <decision> — <one-line why>. <Reversible|Flag for review>.` TIER is L (decide-and-log) or P (propose-and-proceed, needs review); Tier-B decisions are never here (those were asked). This comment is instructions only — real entries go below it as plain `-` list items, so extraction and PR rendering stay clean. -->


## Dependency Safety

[Auto-populated at PR time when this issue's implementation adds or upgrades dependencies. One row per package: `package@version`, publish date, advisories checked, fallback reason if applicable, override justification if applicable. Leave this section blank if no dependency changes were made.]
